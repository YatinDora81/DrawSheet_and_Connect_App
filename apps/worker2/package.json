{
  "name": "worker2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "pnpm run build:prisma && tsc -b",
    "build:prisma": "cd ../../packages/db && npx prisma generate && cd ../../apps/worker2",
    "start": "node ./dist/index.js",
    "dev": "tsc -b && npm run start"
  },
  "dependencies": {
    "@repo/db": "workspace:*",
    "dotenv": "^17.2.1",
    "express": "^4.21.2",
    "mongoose": "^8.18.0"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/express": "^5.0.3",
    "@types/node": "^22",
    "typescript": "^5.5.4"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module"
}
