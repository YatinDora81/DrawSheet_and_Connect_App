{
  "name": "worker2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "pnpm run build:prisma && tsc -b",
    "build:prisma" : "cd .. && cd .. && cd ./packages/db && npx prisma generate && cd .. && cd .. && cd ./apps/worker2",
    "start:prisma": "npx prisma generate && npm run dev",
    "start": "node ./dist/index.js",
    "dev": "npm run build && npm run start"
  },
  "dependencies": {
    "@repo/db": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/express": "^5.0.3",
    "dotenv": "^17.2.1",
    "express": "^4.21.2",
    "mongoose": "^8.18.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module"
}
